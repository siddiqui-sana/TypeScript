function sum(n1: number, n2: number) //We can assign types to the function parameters
{ 
    console.log(n1+n2)
}

const number1 = 5; //If in case of Js 5 would be '5', then sum would have been "53.8"
const number2 = 3.8;

sum(number1, number2);
console.log("Oh now I know how it works")

